[rule]
description = standard text checks followed by email syntax

automatic_field_types = text
automatic_field_name_patterns =
    email
    mail

[composite_rule]
rules =
    standard_text_composite
    email_base
