[rule]
description = look for and report stored text spanning three or more code-point buckets

[base_rule]
# Report a value when it spans at least this many suspicious code-point buckets.
suspicious_buckets_threshold = 3
